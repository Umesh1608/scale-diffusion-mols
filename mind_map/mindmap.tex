% MolSSD Project Mind Map
% Compile with: pdflatex mindmap.tex
\documentclass[tikz,border=10mm]{standalone}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb}
\usetikzlibrary{mindmap,backgrounds,calc,arrows.meta,decorations.pathmorphing,positioning}

% ── colour palette ──────────────────────────────────────────────────
\definecolor{coreOrange}{HTML}{E8590C}     % central node
\definecolor{brTheory}{HTML}{1971C2}       % Branch 1 – Theoretical Foundations
\definecolor{brMath}{HTML}{2F9E44}         % Branch 2 – Mathematical Framework
\definecolor{brArch}{HTML}{9C36B5}         % Branch 3 – Architecture
\definecolor{brScale}{HTML}{E67700}        % Branch 4 – Multi-Scale Hierarchy
\definecolor{brData}{HTML}{0CA678}         % Branch 5 – Data & Infrastructure
\definecolor{brUV}{HTML}{D6336C}           % Branch 6 – UV-Absorber Application
\definecolor{brPipe}{HTML}{4263EB}         % Branch 7 – Experimental Pipeline
\definecolor{brGaps}{HTML}{F76707}         % Branch 8 – Research Gaps
\definecolor{brBase}{HTML}{495057}         % Branch 9 – Baselines & Metrics

\begin{document}
\begin{tikzpicture}[
  mindmap,
  every node/.style={concept, execute at begin node=\hskip0pt},
  grow cyclic,
  text=white,
  level 1/.append style={
    level distance=6.8cm,
    sibling angle=40,
    font=\small\bfseries,
    minimum size=2.6cm,
    text width=2.4cm,
  },
  level 2/.append style={
    level distance=4.2cm,
    sibling angle=38,
    font=\scriptsize,
    minimum size=1.6cm,
    text width=1.5cm,
  },
  level 3/.append style={
    level distance=3.0cm,
    sibling angle=34,
    font=\tiny,
    minimum size=1.1cm,
    text width=1.0cm,
  },
  level 4/.append style={
    level distance=2.4cm,
    sibling angle=30,
    font=\tiny,
    minimum size=0.9cm,
    text width=0.8cm,
  },
]

% ════════════════════════════════════════════════════════════════════
% CENTRAL NODE
% ════════════════════════════════════════════════════════════════════
\node [concept, concept color=coreOrange, font=\large\bfseries,
       minimum size=3.6cm, text width=3.2cm, text=white]
  {\textbf{MolSSD}\\[2pt]Molecular\\Scale-Space\\Diffusion}
% ────────────────────────────────────────────────────────────────────
% BRANCH 1  –  Theoretical Foundations  (≈ 100°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brTheory, grow=148] {
    node (theory) {Theoretical Foundations}
    child [grow=175] {
      node (sst) {Scale-Space Theory}
      child { node {Lindeberg, Witkin, Koenderink} }
      child { node {Gaussian Blurring} }
      child { node (heateq) {Heat Equation $\partial_t u = \Delta u$} }
    }
    child [grow=145] {
      node {Diffusion Models}
      child { node {DDPM (Ho 2020)} }
      child { node {Score SDE (Song 2021)} }
      child { node {Forward / Reverse Process} }
    }
    child [grow=120] {
      node (sgt) {Spectral Graph Theory}
      child { node {Laplacian Eigen\-decomposition} }
      child { node {Spectral Clustering} }
    }
    child [grow=95] {
      node {Information Theory}
      child { node {Mutual Information $I(t)$} }
      child { node {$\mathrm{Info}(t) \!\leftrightarrow\! \mathrm{Info}_{\mathrm{mol}}(r)$} }
    }
    child [grow=200] {
      node {Renormali\-zation Group}
      child { node {Kadanoff Blocking} }
      child { node {Wilson RG} }
      child { node {Coarse-Graining Analogy} }
    }
    child [grow=225] {
      node (ssdpaper) {SSD Paper (2603.08709)}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 2  –  Mathematical Framework  (≈ 60°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brMath, grow=100] {
    node (math) {Mathematical Framework}
    child [grow=130] {
      node {Forward Process $x_t\!=\!M_{1:t}x_0\!+\!\sigma_t\varepsilon$}
    }
    child [grow=105] {
      node (speccoarse) {Spectral Coarsening $M_t$}
    }
    child [grow=80] {
      node (graphlap) {Graph Laplacian $L\!=\!D\!-\!A$}
    }
    child [grow=55] {
      node {Non-Isotropic Posterior (Lanczos)}
    }
    child [grow=35] {
      node {SE(3) Equivariance}
      child { node {CoM Aggregation} }
      child { node {Local Frames} }
    }
    child [grow=150] {
      node {Resolution Schedule (ConvexDecay 0.5)}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 3  –  Architecture  (≈ 20°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brArch, grow=50] {
    node (arch) {Architecture: Molecular Flexi-Net}
    child [grow=85] {
      node {Hierarchical EGNN}
      child { node {Level 0: 2 blk, 512 ch, $N_{\min}$} }
      child { node {Level 1: 3 blk, 384 ch} }
      child { node {Level 2: 3 blk, 256 ch} }
      child { node {Level L: 4 blk, 128 ch, $N$} }
    }
    child [grow=55] {
      node {Dynamic Level Activation}
    }
    child [grow=30] {
      node {Zero Skip Connections}
    }
    child [grow=5] {
      node (uvcond) {UV Conditioning}
      child { node {Cross-Attention} }
      child { node {adaLN} }
      child { node {Classifier-Free Guidance} }
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 4  –  Multi-Scale Hierarchy  (≈ -20°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brScale, grow=-5] {
    node (hierarchy) {Multi-Scale Molecular Hierarchy}
    child [grow=20] {
      node {Scale 0: Full Atomic (all atoms, bond angles)}
    }
    child [grow=0] {
      node {Scale 1: Functional Groups (${\sim}N/3$)}
    }
    child [grow=-20] {
      node {Scale 2: Fragment Level (${\sim}N/6$)}
    }
    child [grow=-40] {
      node (chromo) {Scale 3: Chromo\-phore Skeleton (${\sim}N/12$)}
    }
    child [grow=-60] {
      node {Scale $L$: Single Centroid}
    }
    child [grow=40] {
      node {Chromophore $\to$ D/A Sub\-stituents $\to$ Full Molecule}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 5  –  Data & Infrastructure  (≈ -65°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brData, grow=-60] {
    node (data) {Data \& Infra\-structure}
    child [grow=-25] {
      node (omol) {OMol25 (140M DFT, 83M mols, 2--350 atoms)}
      child { node (pretrain) {Pre-training Backbone} }
      child { node {eSEN Oracle (50.7M params, 1.35\,kcal/mol)} }
      child { node {Scalability Benchmark} }
    }
    child [grow=-60] {
      node {QM9 (130K, 9 heavy atoms) -- Validation}
    }
    child [grow=-85] {
      node {GEOM-Drugs (317K, ${\sim}50$ atoms) -- Scaling}
    }
    child [grow=-110] {
      node (uvdata) {UV Absorber Dataset (50K--200K, ChEMBL/ PubChem/ ZINC)}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 6  –  UV-Absorber Application  (≈ -115°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brUV, grow=-115] {
    node (uvapp) {UV-Absorber Application}
    child [grow=-75] {
      node {Commercial UV Absorbers}
      child { node {Benzo\-triazoles (Tinuvin 326/7/8, UVA)} }
      child { node {Benzo\-phenones (Chimassorb 81)} }
      child { node {Triazines (Tinuvin 1577)} }
      child { node {Cyano\-acrylates (Uvinul 3039, UVB)} }
    }
    child [grow=-115] {
      node (uvtarget) {Target Properties}
      child { node {Broad UV 280--400\,nm} }
      child { node {$\varepsilon > 20{,}000$ L/mol/cm} }
      child { node {Photo\-stability} }
      child { node {$T_d > 300\,{}^{\circ}$C} }
      child { node {Coating Compat\-ibility} }
    }
    child [grow=-145] {
      node {Applications: Drones, Vehicles, Outdoor Infra}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 7  –  Experimental Pipeline  (≈ -165°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brPipe, grow=-165] {
    node (pipe) {Experimental Pipeline}
    child [grow=-125] {
      node {Phase 1: Foundation (QM9, GEOM-Drugs)}
    }
    child [grow=-145] {
      node {Phase 2: OMol25 Pre-train + Scale}
    }
    child [grow=-165] {
      node {Phase 3: UV Dataset + TD-DFT}
    }
    child [grow=-185] {
      node {Phase 4: Conditional MolSSD Training}
    }
    child [grow=-200] {
      node (phase5) {Phase 5: Generate 10K+ $\to$ eSEN $\to$ TD-DFT $\to$ Top 50}
    }
    child [grow=-220] {
      node {Phase 6: Wet-Lab Synthesis, UV-Vis, Coating Tests}
    }
    child [grow=-240] {
      node {Phase 7: Nature Paper}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 8  –  Research Gaps  (≈ 165°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brGaps, grow=195] {
    node (gaps) {Research Gaps (7 Gaps)}
    child [grow=240] {
      node {Gap 1: No Scale-Space in Mol-Gen $\to$ SSD coarsening}
    }
    child [grow=220] {
      node {Gap 2: Flat Diffusion $\to$ Hierarchical MolSSD}
    }
    child [grow=200] {
      node {Gap 3: Atom Limit ${\sim}50$ $\to$ OMol25 scaling}
    }
    child [grow=180] {
      node {Gap 4: No UV Conditioning $\to$ $\lambda$-guided gen}
    }
    child [grow=160] {
      node {Gap 5: No Energy Oracle $\to$ eSEN from OMol25}
    }
    child [grow=140] {
      node {Gap 6: No Spec\-tral Sched\-ule $\to$ Convex\-Decay}
    }
    child [grow=260] {
      node {Gap 7: Theory Gap $\to$ Info-theoretic proof}
    }
  }
% ────────────────────────────────────────────────────────────────────
% BRANCH 9  –  Baselines & Metrics  (≈ -235°)
% ────────────────────────────────────────────────────────────────────
  child [concept color=brBase, grow=-250] {
    node (baselines) {Baselines \& Metrics}
    child [grow=-220] {
      node {Baselines}
      child { node {EDM, GeoLDM} }
      child { node {GCDM, EQGAT-diff} }
      child { node {MolDiff, HierDiff} }
    }
    child [grow=-250] {
      node {Molecular Metrics}
      child { node {Validity, Unique\-ness, Novelty} }
      child { node {JS Diver\-gence} }
      child { node {QED, SA Score} }
    }
    child [grow=-280] {
      node (uvmetrics) {UV Metrics}
      child { node {Absorption Accuracy} }
      child { node {Photo\-stability} }
      child { node {Coating Compat\-ibility} }
    }
  }
; % end of mindmap tree

% ════════════════════════════════════════════════════════════════════
% CROSS-BRANCH CONNECTIONS  (dashed lines linking related concepts)
% ════════════════════════════════════════════════════════════════════
\begin{pgfonlayer}{background}

  % Spectral Graph Theory ↔ Spectral Coarsening Operator
  \draw [dashed, brTheory!70, line width=0.8pt, shorten >=4pt, shorten <=4pt]
    (sgt) -- (speccoarse)
    node [midway, fill=white, font=\tiny, text=brTheory!90, inner sep=1pt, rounded corners]
    {spectral link};

  % Spectral Graph Theory ↔ Graph Laplacian
  \draw [dashed, brTheory!50, line width=0.6pt, shorten >=4pt, shorten <=4pt]
    (sgt) -- (graphlap);

  % OMol25 ↔ Pre-training (already parent–child, add emphasis)
  % Instead: OMol25 ↔ Pipeline Phase 2
  \draw [dashed, brData!70, line width=0.8pt, shorten >=6pt, shorten <=6pt]
    (omol) to[bend left=20] (pretrain);

  % Chromophore skeleton ↔ UV-Absorber Application
  \draw [dashed, brScale!70, line width=0.8pt, shorten >=6pt, shorten <=6pt]
    (chromo) to[bend right=15] (uvapp)
    node [midway, fill=white, font=\tiny, text=brScale!90, inner sep=1pt, rounded corners]
    {chromophore $\to$ UV};

  % UV Conditioning (architecture) ↔ UV Target Properties
  \draw [dashed, brArch!60, line width=0.7pt, shorten >=6pt, shorten <=6pt]
    (uvcond) to[bend right=25] (uvtarget)
    node [pos=0.45, fill=white, font=\tiny, text=brArch!90, inner sep=1pt, rounded corners]
    {$\lambda$-conditioning};

  % UV dataset ↔ UV Application
  \draw [dashed, brData!50, line width=0.6pt, shorten >=5pt, shorten <=5pt]
    (uvdata) -- (uvapp);

  % SSD Paper ↔ Mathematical Framework
  \draw [dashed, brTheory!60, line width=0.7pt, shorten >=5pt, shorten <=5pt]
    (ssdpaper) to[bend left=15] (math)
    node [midway, fill=white, font=\tiny, text=brTheory!80, inner sep=1pt, rounded corners]
    {unifies};

  % Pipeline Phase 5 ↔ UV Metrics
  \draw [dashed, brPipe!50, line width=0.6pt, shorten >=5pt, shorten <=5pt]
    (phase5) to[bend right=15] (uvmetrics);

  % Research Gaps ↔ Theory
  \draw [dashed, brGaps!40, line width=0.5pt, shorten >=8pt, shorten <=8pt]
    (gaps) to[bend left=12] (theory);

  % Data ↔ Baselines
  \draw [dashed, brBase!40, line width=0.5pt, shorten >=8pt, shorten <=8pt]
    (data) to[bend right=10] (baselines);

  % Architecture ↔ Multi-Scale Hierarchy
  \draw [dashed, brArch!40, line width=0.5pt, shorten >=8pt, shorten <=8pt]
    (arch) to[bend right=8] (hierarchy)
    node [midway, fill=white, font=\tiny, text=brArch!70, inner sep=1pt, rounded corners]
    {level $\leftrightarrow$ scale};

\end{pgfonlayer}

% ════════════════════════════════════════════════════════════════════
% TITLE
% ════════════════════════════════════════════════════════════════════
\node [above=0.3cm of current bounding box.north,
       font=\Large\bfseries\sffamily, text=coreOrange!90!black]
  {MolSSD Project --- Complete Mind Map};

\end{tikzpicture}
\end{document}
